import { Component } from '@angular/core';

@Component({
  selector: 'schedule',
  styleUrls: ['schedule.component.scss'],
  template: `
    <div>
      Schedule
    </div>
  `
})
export class ScheduleComponent {
  constructor() {}
}